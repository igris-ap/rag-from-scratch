# Copy this file to .env and fill in your values
# (currently all config is hardcoded in each file — use this as a reference)

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rag
DB_USER=rag
DB_PASSWORD=rag

# Ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
EMBED_MODEL=all-MiniLM-L6-v2

# App
GRADIO_PORT=7860
MAX_HISTORY=10
